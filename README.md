# EnglobeDocs — Prototype

This repository contains the initial skeleton for the EnglobeDocs XAMPP project.

Structure (partial):

- englobedocs/
  - index.php
  - login.php
  - register.php
  - dashboard.php
  - admin.php
  - logout.php
  - config/database.php
  - controllers/
  - models/
  - views/
  - uploads/
  - assets/
    - css/
    - js/
    - images/
    - icons/

- database.sql — initial SQL schema to create database `englobedocs` and core tables.

Instructions: This is an initial commit to let us create a feature branch `englobedocs-init`. I'll add minimal starter files and the SQL schema in this commit. After this commit I'll create the requested branch and continue adding full project files there.
